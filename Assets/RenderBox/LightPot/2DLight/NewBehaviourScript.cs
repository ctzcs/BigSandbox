using UnityEngine;

namespace Box2.LightPot._2DLight
{
    public class NewBehaviourScript : MonoBehaviour
    {
        private SpriteRenderer sr;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}

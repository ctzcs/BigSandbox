using UnityEngine;

namespace Box2.MusicGame.Scripts
{
    public class AnimCpt
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }

        public void Play(AnimationClip clip,float startTime,float shouldArriveTime)
        {
            
        }
    }
}

using UnityEngine;

namespace CorePlayBox.DMC
{
    public class Buff : MonoBehaviour
    {
        public int a;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
